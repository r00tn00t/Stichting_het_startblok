# public/

Statische bestanden die rechtstreeks geserveerd worden vanaf `/`.

Leg hier het logo neer als **`startblok-logo.png`** — dan verschijnt het automatisch
in de header (linksboven) en op de loginpagina. Is het er niet, dan toont de app
gewoon de tekst "ZwemStart" als fallback.
